version: '3'
services:
  mysql:
    image: mysql:8.0
    ports:
      - "3307:3306"
    environment:
      - MYSQL_USERNAME=mysql
      - MYSQL_PASSWORD=mysql
      - MYSQL_DB=news_service_db
    volumes:
      - .init.sql:/docker-entrypoint-initdb.d/init.sql